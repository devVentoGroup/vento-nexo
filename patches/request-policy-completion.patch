diff --git a/src/app/inventory/remissions/actions.ts b/src/app/inventory/remissions/actions.ts
--- a/src/app/inventory/remissions/actions.ts
+++ b/src/app/inventory/remissions/actions.ts
@@
   let items: Array<{
@@
     request_policy_id: string | null;
+    request_policy_kind: string | null;
+    request_policy_label: string | null;
     requested_policy_qty: number | null;
+    request_unit_code: string | null;
+    base_qty_per_request_unit: number | null;
+    request_constraint_mode: string | null;
+    request_minimum_qty: number | null;
+    request_step_qty: number | null;
+    request_allow_fraction: boolean | null;
   }> = [];
@@
             requires_package_dispatch: false,
             request_policy_id: requestPolicy.id,
+            request_policy_kind: requestPolicy.policyKind,
+            request_policy_label: requestPolicy.label,
             requested_policy_qty: requestedPolicyQty,
+            request_unit_code: requestPolicy.requestUnitCode,
+            base_qty_per_request_unit: requestPolicy.baseQtyPerRequestUnit,
+            request_constraint_mode: requestPolicy.constraintMode,
+            request_minimum_qty: requestPolicy.minimumRequestQty,
+            request_step_qty: requestPolicy.requestStepQty,
+            request_allow_fraction: requestPolicy.allowFraction,
           };
         }
+
+        throw new Error(
+          "Este producto no tiene una política de solicitud activa. Configúrala en Catálogo antes de crear la remisión.",
+        );
 
         const stockUnitCode = normalizeUnitCode(
@@
           requires_package_dispatch: productUsesPackages,
           request_policy_id: null,
+          request_policy_kind: null,
+          request_policy_label: null,
           requested_policy_qty: null,
+          request_unit_code: null,
+          base_qty_per_request_unit: null,
+          request_constraint_mode: null,
+          request_minimum_qty: null,
+          request_step_qty: null,
+          request_allow_fraction: null,
         };
@@
     requires_package_dispatch: item.requires_package_dispatch,
     request_policy_id: item.request_policy_id,
+    request_policy_kind: item.request_policy_kind,
+    request_policy_label: item.request_policy_label,
     requested_policy_qty: item.requested_policy_qty,
+    request_unit_code: item.request_unit_code,
+    base_qty_per_request_unit: item.base_qty_per_request_unit,
+    request_constraint_mode: item.request_constraint_mode,
+    request_minimum_qty: item.request_minimum_qty,
+    request_step_qty: item.request_step_qty,
+    request_allow_fraction: item.request_allow_fraction,
   }));
