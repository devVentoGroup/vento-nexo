// Triggered after the temporary workflow became available on preview.
import fs from "node:fs";

function patch(path, replacements) {
  let source = fs.readFileSync(path, "utf8");
  for (const [name, before, after] of replacements) {
    if (!source.includes(before)) throw new Error(`${path}: missing ${name}`);
    source = source.replace(before, after);
  }
  fs.writeFileSync(path, source);
}

patch("src/app/inventory/remissions/page.tsx", [
  ["area search param", `  from_site_id?: string;\n  new?: string;`, `  from_site_id?: string;\n  area_kind?: string;\n  new?: string;`],
  ["privileged area flag", `  const effectiveRole = (\n    canUseRoleOverride ? roleOverride : actualRole\n  ).toLowerCase();`, `  const effectiveRole = (\n    canUseRoleOverride ? roleOverride : actualRole\n  ).toLowerCase();\n  const canChooseRequestArea =\n    !isSharedDevice &&\n    ["propietario", "gerente_general"].includes(actualRole.trim().toLowerCase());\n  let selectedRequestAreaKind = canChooseRequestArea\n    ? String(sp.area_kind ?? "").trim()\n    : "";`],
  ["preserve area href", `    if (selectedFromSiteId) params.set("from_site_id", selectedFromSiteId);\n    if (opts?.showCreate) params.set("new", "1");`, `    if (selectedFromSiteId) params.set("from_site_id", selectedFromSiteId);\n    if (selectedRequestAreaKind) params.set("area_kind", selectedRequestAreaKind);\n    if (opts?.showCreate) params.set("new", "1");`],
  ["manager all area scope", `          canSeeAllAreas: canViewAll,\n        })\n      : await resolveOperationalRemissionAreaScope({\n          supabase,\n          userId: user.id,\n          siteId: productFilterSiteId,\n          canSeeAllAreas: canViewAll,`, `          canSeeAllAreas: canViewAll || canChooseRequestArea,\n        })\n      : await resolveOperationalRemissionAreaScope({\n          supabase,\n          userId: user.id,\n          siteId: productFilterSiteId,\n          canSeeAllAreas: canViewAll || canChooseRequestArea,`],
  ["selected request area", `  const requestedAreaKind = requestAreaScope?.defaultAreaKind ?? "";`, `  const selectableRequestAreaKinds = new Set(areaOptions.map((option) => option.value));\n  if (canChooseRequestArea) {\n    if (!selectableRequestAreaKinds.has(selectedRequestAreaKind)) {\n      const activeKind = String(requestAreaScope?.activeAreaKind ?? "").trim();\n      selectedRequestAreaKind = selectableRequestAreaKinds.has(activeKind)\n        ? activeKind\n        : (areaOptions.find((option) => option.value !== "general")?.value ??\n          areaOptions[0]?.value ??\n          "");\n    }\n  } else {\n    selectedRequestAreaKind = requestAreaScope?.defaultAreaKind ?? "";\n  }\n  const requestedAreaKind = selectedRequestAreaKind;`],
  ["create area selector", `            <Link\n              href={buildHubHref({ showCreate: true })}\n              className="ui-btn ui-btn--brand h-11 px-4 text-sm font-semibold"\n            >\n              Crear solicitud\n            </Link>`, `            {canChooseRequestArea && areaOptions.length > 1 ? (\n              <form method="get" className="flex flex-wrap items-end gap-2">\n                {activeSiteId ? <input type="hidden" name="site_id" value={activeSiteId} /> : null}\n                {selectedFromSiteId ? (\n                  <input type="hidden" name="from_site_id" value={selectedFromSiteId} />\n                ) : null}\n                <input type="hidden" name="new" value="1" />\n                <label className="flex min-w-[190px] flex-col gap-1">\n                  <span className="ui-label">Área solicitante</span>\n                  <select\n                    name="area_kind"\n                    defaultValue={selectedRequestAreaKind}\n                    className="ui-input h-11"\n                    required\n                  >\n                    {areaOptions.map((option) => (\n                      <option key={option.value} value={option.value}>\n                        {option.label}\n                      </option>\n                    ))}\n                  </select>\n                </label>\n                <button className="ui-btn ui-btn--brand h-11 px-4 text-sm font-semibold">\n                  Crear solicitud\n                </button>\n              </form>\n            ) : (\n              <Link\n                href={buildHubHref({ showCreate: true })}\n                className="ui-btn ui-btn--brand h-11 px-4 text-sm font-semibold"\n              >\n                Crear solicitud\n              </Link>\n            )}`],
  ["modal area caption", `                  Solicitud desde {activeSiteName}. El formulario se abre\n                  encima del historial; cierra para volver a la lista.`, `                  Solicitud desde {activeSiteName}\n                  {selectedRequestAreaKind\n                    ? \` · Área: \${formatOperationalRemissionAreaLabel(selectedRequestAreaKind)}\`\n                    : ""}. El formulario se abre encima del historial; cierra para volver a la lista.`],
]);

patch("src/app/inventory/remissions/actions.ts", [
  ["load employee role", `  const inventoryPostingEnabled = await readBooleanAppSetting(\n    supabase,\n    REMISSIONS_INVENTORY_POSTING_SETTING_KEY,\n    false,\n  );`, `  const inventoryPostingEnabled = await readBooleanAppSetting(\n    supabase,\n    REMISSIONS_INVENTORY_POSTING_SETTING_KEY,\n    false,\n  );\n  const { data: requestingEmployee } = operationalSession.isSharedDevice\n    ? { data: null }\n    : await supabase\n        .from("employees")\n        .select("role")\n        .eq("id", user.id)\n        .maybeSingle();\n  const requestingRole = String(\n    operationalSession.role ?? requestingEmployee?.role ?? "",\n  )\n    .trim()\n    .toLowerCase();\n  const canChooseRequestArea =\n    !operationalSession.isSharedDevice &&\n    ["propietario", "gerente_general"].includes(requestingRole);`],
  ["manager operational context", `  let activeAreaId = operationalSession.areaId;\n  if (!operationalSession.isSharedDevice) {\n    const opContext = await getOperationalContext({\n      supabase,\n      employeeId: user.id,\n      siteId: toSiteId,\n      appCode: APP_ID,\n    });\n    if (!opContext?.can_operate) {\n      redirect(\n        "/inventory/remissions?error=" +\n          encodeURIComponent(\n            buildOperationalBlockMessage(\n              opContext,\n              "No puedes solicitar remisiones en este momento para esta sede.",\n            ),\n          ),\n      );\n    }\n    activeAreaId = opContext.active_area_id;\n  }`, `  let activeAreaId = operationalSession.areaId;\n  if (!operationalSession.isSharedDevice && !canChooseRequestArea) {\n    const opContext = await getOperationalContext({\n      supabase,\n      employeeId: user.id,\n      siteId: toSiteId,\n      appCode: APP_ID,\n    });\n    if (!opContext?.can_operate) {\n      redirect(\n        "/inventory/remissions?error=" +\n          encodeURIComponent(\n            buildOperationalBlockMessage(\n              opContext,\n              "No puedes solicitar remisiones en este momento para esta sede.",\n            ),\n          ),\n      );\n    }\n    activeAreaId = opContext.active_area_id;\n  }`],
  ["manager request permission", `  const canRequest = operationalSession.isSharedDevice\n    ? await checkOperationalSessionPermission({\n        supabase,\n        session: operationalSession,\n        appId: APP_ID,\n        code: PERMISSIONS.remissionsRequest,\n      })\n    : await checkOperationalPermission({\n        supabase,\n        permissionCode: \`${APP_ID}.${PERMISSIONS.remissionsRequest}\`,\n        siteId: toSiteId,\n        areaId: activeAreaId,\n        appCode: APP_ID,\n      });`, `  const canRequest = operationalSession.isSharedDevice\n    ? await checkOperationalSessionPermission({\n        supabase,\n        session: operationalSession,\n        appId: APP_ID,\n        code: PERMISSIONS.remissionsRequest,\n      })\n    : canChooseRequestArea\n      ? await checkPermissionWithRoleOverride({\n          supabase,\n          appId: APP_ID,\n          code: PERMISSIONS.remissionsRequest,\n          context: { siteId: toSiteId },\n          actualRole: requestingRole,\n        })\n      : await checkOperationalPermission({\n          supabase,\n          permissionCode: \`${APP_ID}.${PERMISSIONS.remissionsRequest}\`,\n          siteId: toSiteId,\n          areaId: activeAreaId,\n          appCode: APP_ID,\n        });`],
  ["manager area validation", `      : await resolveOperationalRemissionAreaScope({\n          supabase,\n          userId: user.id,\n          siteId: toSiteId,\n          canSeeAllAreas: false,\n        });\n  if (requestAreaScope.blockedReason) {`, `      : await resolveOperationalRemissionAreaScope({\n          supabase,\n          userId: user.id,\n          siteId: toSiteId,\n          canSeeAllAreas: canChooseRequestArea,\n        });\n  if (requestAreaScope.blockedReason) {`],
  ["single selected area", `  const requestedRoleAreaKind = requestAreaScope.defaultAreaKind;\n  const invalidAreaItems = items.filter((item) => {`, `  const submittedAreaKinds = Array.from(\n    new Set(\n      items\n        .map((item) => String(item.production_area_kind ?? "").trim())\n        .filter(Boolean),\n    ),\n  );\n  if (submittedAreaKinds.length > 1) {\n    redirect(\n      "/inventory/remissions?error=" +\n        encodeURIComponent("Una solicitud debe pertenecer a una sola área solicitante."),\n    );\n  }\n  const submittedAreaKind = submittedAreaKinds[0] ?? "";\n  if (\n    canChooseRequestArea &&\n    submittedAreaKind &&\n    requestAreaScope.enabledAreaKinds.length > 0 &&\n    !requestAreaScope.enabledAreaKinds.includes(submittedAreaKind)\n  ) {\n    redirect(\n      "/inventory/remissions?error=" +\n        encodeURIComponent("El área seleccionada no está habilitada para remisiones en esta sede."),\n    );\n  }\n  const requestedRoleAreaKind = canChooseRequestArea\n    ? submittedAreaKind\n    : requestAreaScope.defaultAreaKind;\n  const invalidAreaItems = items.filter((item) => {`],
]);

console.log("Privileged request area patch applied");
